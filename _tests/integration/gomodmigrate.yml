format_version: 8
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

workflows:
  test:
    after_run:
    - gopath
    - gomod

  gopath:
    steps:
    - npm@1.1.4:
        inputs:
        - command: --version

  gomod:
    steps:
    - path::./testdata/gomodmigrate: { }
