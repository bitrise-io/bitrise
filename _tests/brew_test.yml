format_version: 0.9.8
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib
app:
  envs: []
workflows:
  test:
    title: Workflow for master branch
    envs: []
    steps:
    - script:
        title: Test
        inputs:
        - content: |-
            #!/bin/bash
            set -e
            echo "Test - OK" > brew.test.file
